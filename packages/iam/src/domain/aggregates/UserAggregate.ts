// User aggregate root, `entities/User` içinde tanımlıdır (AggregateRoot'tan türer,
// domain event üretir). Aggregate adıyla erişim için alias re-export.
export { User as UserAggregate } from '../entities/User';
