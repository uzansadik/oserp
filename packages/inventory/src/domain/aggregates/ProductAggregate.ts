// Product aggregate root, `entities/Product` içinde tanımlıdır (AggregateRoot'tan türer,
// domain event üretir). Aggregate adıyla erişim için alias re-export.
export { Product as ProductAggregate } from '../entities/Product';
